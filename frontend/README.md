# Foody
foody is an online food delivery service like zomato and swiggy.

## Prerequisites
* nodejs

## Installing
* `git clone url`
* `cd foody-frontend`
* `npm install`
* `npm start`

## Deployment
* `npm run build`
* copy the build folder to the server
